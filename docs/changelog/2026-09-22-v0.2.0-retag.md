# 重打 v0.2.0 tag，发布日志补全 v0.1.0 以来的全部改动

- 日期：2026-09-22
- 对比基线：`6fbea79`（MCP：按需启动（start = "lazy"），修复 npx 卡住时的误导提示）
- 对应里程碑：无（发版）

## 功能变化

- 发版：`v0.2.0` tag 从 `a7b39da` 移到本次提交，把 MCP 按需启动和 npx 提示修复也收进 0.2.0。
- 文档：重写 `docs/releases/v0.2.0.md`，补全 v0.1.0 以来的全部改动。原稿每个里程碑只写了几行，
  漏了新依赖 `croniter`、仓库地址变化、新 SSE 帧、各配置段的字段、新数据目录、会话 JSONL 格式变化等。新版：
  - 开头加「一览」表：新命令、REPL 命令、配置段、依赖、数据目录、事件 / SSE 帧、仓库地址、测试数
  - M5 / M6 / M4 / debug / 工作台各节补细节（配置字段、启动与崩溃行为、各档 debug 输出、Markdown 支持范围等）
  - 新增「修复」一节，集中列 6 个修复
  - 「升级」补 `uv sync`、Ollama `num_ctx`、开发目录初始化、tag 重打要 `--reinstall`；新增「退回 v0.1.0 时要注意」
  - 「质量」补测试数对比（314 → 604）、`node` 依赖、已知偶发失败的测试
  - 文末列出 8 份详细变更记录和相关笔记的链接

## 函数级改动

- 无（`src/` 未改动）

## 配置与依赖

- **需要手动处理**：日常用的 `sa` 如果是之前从 `@v0.2.0` 装的，重装一遍才能用 `start = "lazy"`：
  `uv tool install --reinstall "git+https://github.com/tjiisztiuv/SimpleAgent.git@v0.2.0"`。
- 本地 clone 里如果已有旧的 `v0.2.0` tag，`git fetch --tags --force` 才会更新到新位置。

## 测试

- 测试结果：`uv run pytest -q` 604 passed；`ruff check`、`ruff format --check` 通过。
